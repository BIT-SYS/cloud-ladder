# This workflow will build a Java project with Gradle
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: Java CI with Gradle

on:
  push:
    branches:
      - develop
      - ast
  pull_request:
    branches:
      - develop

jobs:
  grammar-test:
    
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: setup environment for antlr
      run: |
        sudo curl https://www.antlr.org/download/antlr-4.7.1-complete.jar -o /usr/local/lib/antlr-complete.jar
        sudo tee -a /usr/bin/grun > /dev/null << 'EOF'
        \#!bash
        java -cp .:/usr/local/lib/antlr-complete.jar org.antlr.v4.gui.TestRig "$@"
        EOF
        sudo chmod 777 /usr/bin/grun
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew  
    - name: Test grammar with gradle
      run: ./gradlew testGrammar --scan

  unit-test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew  
    - name: Test with Gradle
      run: ./gradlew test --scan
      

